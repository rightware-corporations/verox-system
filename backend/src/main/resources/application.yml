spring:
  application:
    name: verox-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/verox}
    username: ${DB_USERNAME:verox}
    password: ${DB_PASSWORD:verox_dev}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    root: INFO
